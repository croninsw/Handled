@model IEnumerable<Handled.Models.BicycleRider>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cyclist)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bicycle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cyclist.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bicycle.VIN)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BicycleRiderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BicycleRiderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BicycleRiderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
