@model Handled.Models.Incident

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Incident</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="BicycleRiderId" class="control-label"></label>
                <select asp-for="BicycleRiderId" class ="form-control" asp-items="ViewBag.BicycleRiderId"></select>
            </div>
            <div class="form-group">
                <label asp-for="CarDriverId" class="control-label"></label>
                <select asp-for="CarDriverId" class ="form-control" asp-items="ViewBag.CarDriverId"></select>
            </div>
            <div class="form-group">
                <label asp-for="IncidentDate" class="control-label"></label>
                <input asp-for="IncidentDate" class="form-control" />
                <span asp-validation-for="IncidentDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
